/**
 * The one breakpoint every primitive in this folder agrees on: where the
 * desktop design takes over. It comes from fluid.config.json (bands.desktop)
 * through the generated fluid.ts, so it cannot drift from the CSS.
 */
export { DESKTOP_PX as ENGAGE_BREAKPOINT_PX, DESKTOP_QUERY as ENGAGE_QUERY } from '@/styles/fluid/fluid'
