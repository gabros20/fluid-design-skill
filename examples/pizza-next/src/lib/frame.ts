/**
 * The ONE page frame: cap and gutter on the same box (frame-and-gutter.md §1).
 * Canvas 1680, gutter 80, from fluid.config.json. Mobile keeps a plain 24/32px
 * gutter; the fluid classes exist only from `lg`.
 */
export const FRAME = 'lg:fluid-cap-1680 lg:fluid-px-80 mx-auto w-full max-w-[1680px] fluid-px-24'
